services:
  backend:
    build:
      context: ./backend
    privileged: true
    pid: host
    environment:
      APP_ENV: production
      APP_DEBUG: "false"
      APP_URL: http://localhost:9003
      TERMINAL_EXECUTION_MODE: host
      TERMINAL_HOST_SHELL: /bin/bash
      TERMINAL_HOST_PID: 1
      TERMINAL_HOST_NSENTER_PATH: /usr/bin/nsenter
      DB_CONNECTION: sqlite
      DB_DATABASE: /var/www/html/storage/app/database.sqlite
      CACHE_STORE: database
      SESSION_DRIVER: database
      QUEUE_CONNECTION: database
      SANCTUM_STATEFUL_DOMAINS: localhost,127.0.0.1,localhost:9003
      MAIL_MAILER: log
      FILESYSTEM_DISK: local
    volumes:
      - backend-storage:/var/www/html/storage
    expose:
      - "8000"
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
      args:
        VITE_API_BASE_URL: /api
    depends_on:
      - backend
    ports:
      - "9003:80"
    restart: unless-stopped

volumes:
  backend-storage:
